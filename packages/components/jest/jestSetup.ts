// Import built-in Jest matchers
import '@testing-library/react-native/matchers'

// Global React import for all test files since Jest requires it while React no longer does
import React from 'react'
global.React = React
